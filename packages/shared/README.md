# WizFlow Shared

Cross-cutting constants and types for `apps/api` and `apps/web`.

- **P0:** Role slugs and event type enums (Python); TypeScript mirrors added in P1.
- **P1+:** Generated types from OpenAPI optional.
