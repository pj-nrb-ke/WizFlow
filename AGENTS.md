# WizFlow — agent instructions

Read [docs/PARALLEL-AGENT-NOTE.md](docs/PARALLEL-AGENT-NOTE.md) first.

**You own:** git push/pull (SSH), builds, Docker, migrations, seed — not the user.

**Git remote:** `git@github.com-pj-nrb-ke:pj-nrb-ke/WizFlow.git` (branch `main`).

**One-time / repair SSH:** `powershell -File scripts/setup-github-ssh.ps1`

**User only verifies** the app at http://localhost:5200 (login: `admin@demo.wizflow.biz` / `changeme`).
