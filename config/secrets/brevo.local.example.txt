# Copy to brevo.local.txt (gitignored) and fill in from app.brevo.com
# Transactional → SMTP & API keys

SMTP_HOST=smtp-relay.brevo.com
SMTP_PORT=587
SMTP_USER=your-brevo-login@example.com
# SMTP key (NOT your account password, NOT the xkeysib API key)
SMTP_PASS=your-xsmtpsib-smtp-key
MAIL_FROM=noreply@yourdomain.com
MAIL_FROM_NAME=WizFlow
# Transactional API key (xkeysib-...) — used first if set; must match verified sender
BREVO_API_KEY=your-xkeysib-api-key
